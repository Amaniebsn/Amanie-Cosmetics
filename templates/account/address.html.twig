{% extends 'base.html.twig' %}

{% block title %}Mes adresses - Amanie Cosmetics {% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
     <a href="{{ path ('app_account_adress_add') }}" class="btn btn-info btn-sm float-right"> Ajouter une adresse</a>
     <h1> Mes adresses </h1>
     C'est dans cet espace que vous pourrez gérer toutes vos adresses.
    <a href="{{ path('account') }}">Retour</a>
   
     <hr>
     {% if app.user.addresses|length == 0 %}
        <p class="text-center">
           Vous n'avez pas encore ajouté d'adresses dans votre compte client, pour en ajouter une merci <a href="" Click here</a>
        </p>
    {% else %}
     {% for address in app.user.addresses %}
    <div class="row">
        
        <div class="col-md-4">
             <div class="card" style="width: 18rem;">
                 <div class="card-body">
                     <h5 class="card-title">{{ address.name }}</h5>
                      <p class="card-text">{{ address.address }}
                           {{ address.address }}, </br>
                           {{ address.postal }} - {{ address.city }} - {{ address.country }}
                      </p>
                    
                   </div>
                 </div>
             </div>
       {% endfor %}
    </div>
{% endif %}   
{% endblock %}